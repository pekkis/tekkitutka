DATABASE_URL=postgres://tekkitutka:tekkitutka@localhost:5432/tekkitutka
# Used by client-side code (browser → host)
PUBLIC_API_URL=http://localhost:3001
# Optional override for server-side fetches (Astro SSR → API). Defaults to PUBLIC_API_URL.
# In docker-compose, set this to http://api:3001
# API_URL=http://localhost:3001
